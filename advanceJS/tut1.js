console.log("Start to learn Advance Js");
//Global Execution:
/*                JavaScript engine create a global execution context before it start to execute the code ans 
                  A new execution context created everytime when function is called*/

//Execution Stack :
         /*       first all code store into the global execution and when any function call
                  create one new execution context and store into the stack after that 
                  the function run completly done then its remove from the stack. */